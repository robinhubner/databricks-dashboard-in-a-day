# The main pipeline for dab_data_engineering

resources:
  pipelines:
    dab_data_engineering_etl:
      name: dab_data_engineering_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "."

      libraries:
        - glob:
            include: transformations/**
